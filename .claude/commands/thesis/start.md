---
description: 박사논문 연구 워크플로우 시작 (mode: topic|question|review|learning|proposal)
argument-hint: <mode> <input>
allowed-tools: Task(*), Read(*), Write(*), Bash(*)
context: fork
agent: general-purpose
---

# 박사논문 연구 워크플로우 시작

선택된 모드로 연구 워크플로우를 시작합니다.

## 입력 모드

| Mode | 설명 | 예시 |
|------|------|------|
| `topic` | 연구 주제로 시작 (기본) | `/thesis:start topic AI와 조직혁신` |
| `question` | 연구질문으로 시작 | `/thesis:start question "디지털 전환이 조직학습에 미치는 영향은?"` |
| `review` | 기존 문헌검토 활용 | `/thesis:start review` (user-resource/ 참조) |
| `learning` | 학습모드 시작 | `/thesis:start learning` |
| `proposal` | 프로포절 업로드 시작 | `/thesis:start proposal --proposal-path my-proposal.pdf` |

## 실행 원칙

```
┌─────────────────────────────────────────────────────────────┐
│  🎯 품질 최우선 원칙                                        │
├─────────────────────────────────────────────────────────────┤
│  1. 모든 Sub-agent는 opus 모델 사용                         │
│  2. 모든 실행은 순차(Sequential) 방식                       │
│  3. 모든 출력에 GRA Hook 검증 적용                          │
└─────────────────────────────────────────────────────────────┘
```

## 모드별 처리

### Mode A: topic (기본)
1. `@topic-explorer` 에이전트 호출
2. 연구 주제의 학술적 맥락 분석
3. 연구질문 후보 5-7개 도출
4. HITL-1 체크포인트로 이동

### Mode B: question
1. 입력된 연구질문 검증
2. 바로 문헌검토 단계 진입
3. Wave 1 시작

### Mode C: review
1. `user-resource/` 폴더의 문헌검토 파일 분석
2. `@literature-analyzer` 에이전트 호출
3. 기존 문헌검토 Gap 식별
4. 연구 방향 제안

### Mode D: learning
1. `@methodology-tutor` 에이전트 호출
2. 학습 트랙 선택 안내
3. 8개 트랙 중 선택

### Mode F: proposal
1. 프로포절 파일 확인
2. `@proposal-analyzer` 에이전트 호출
3. 추출 결과 제시 + HITL (`/thesis:review-extracted-plan`)
4. 승인 후 Phase 1 진입 (Flag-and-Follow 정책 적용)

## 세션 상태 확인

워크플로우 시작 전 기존 세션 확인:
- `thesis-output/` 폴더에서 최근 세션 검색
- 기존 세션 발견 시 재개 여부 확인

## 다음 단계

- Mode A: HITL-1 → `/thesis:set-research-question`
- Mode B/C: Wave 1 → 문헌검토 시작
- Mode D: 학습 트랙 선택
- Mode F: `/thesis:review-extracted-plan` → Phase 1

$ARGUMENTS
